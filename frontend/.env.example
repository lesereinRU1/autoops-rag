# Leave empty when using the Vite development proxy or FastAPI /demo build.
# For a separate backend host, use an explicit trusted origin such as:
# VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_API_BASE_URL=
