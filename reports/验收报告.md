# AutoOps RAG V4 验收报告

> **Historical / stale / 历史版本结果。** 本报告保留旧环境验收记录，其中路径、corpus、测试和评测数字不是当前事实。

验收日期：2026-06-29

- 项目路径：`<repository-root>`（历史环境已脱敏）
- Python：3.12.10，虚拟环境在 D 盘
- 数据：3 份已入库 Siemens 官方 PDF（1785 页）+ 3 份中文资料
- 新鲜度：增加 S7-1200 G2 V1.0.1 中文手册；经典 S7-1200 V4.6 明确标记为已被V4.7替代但暂时保留
- 索引：16969 chunks，本地 Qdrant；其中12035个结构化表格行切片，覆盖1861张表、1068个PDF页
- 向量：`BAAI/bge-small-zh-v1.5`，FastEmbed/ONNX，512 维
- 检索：Dense + BM25 + RRF + 轻量重排
- 服务：FastAPI + 浏览器页面
- LangGraph：选择查询工具、检查证据、必要时改写一次问题，并显示处理记录
- 反馈：记录有帮助或无帮助，保存人工确认的方案，并统计使用次数
- 会话范围：最近1到2轮问题只在短追问中用于补全参数名或操作对象，不加载完整聊天历史
- 运行统计：`POST /api/chat`返回总耗时、检索轮数、检索操作数、结构化查询次数、外部模型调用次数和外部token数
- 外部模型：当前未启用，外部模型调用和外部token均为0；兼容客户端已实现一次重试、usage统计和本地抽取式降级
- 多轮追问：最近1到2轮问题参与短追问解析，24小时有效，可手动清空；3组两轮测试全部通过
- 运行评测：单并发P50=249.88毫秒、P95=343.23毫秒；8并发P95=2406.08毫秒、吞吐量=4.668请求/秒
- 并发控制：查询允许并发读，索引重建独占写；接口超时30秒、每分钟300次限流。继续扩容前需要迁移Qdrant Server并处理CPU向量计算瓶颈
- 表格质量：严格线框检测 + 网格占用率 + URL版式过滤；审计中的链接误判为0，扫描件/跨页表/合并单元格仍需人工复核
- 切片审计：完整文本同时用于向量、BM25和证据展示；字符长度P95为1337，最大6597，429个切片超过2000字符；按项目分词规则估算，超过600个词元的有4个。后续应在同一评测集上比较检索文本压缩方案，不能直接套用固定长度
- 索引一致性：清理一次中断重建留下的12494个旧点后，`chunks.jsonl`与Qdrant均为16969条；已增加可重复运行的对账脚本
- 自动测试：12 passed
- 15题小规模gold-chunk消融：Dense Recall@5=0.6000，BM25=0.9333，Hybrid=1.0000；Hybrid nDCG@10=0.8917
- 评测集偏差：未经过独立盲审；10题正确证据来自项目补充资料，5题来自官方手册。官方手册子集Hybrid MRR@10=0.5667、nDCG@10=0.6750
- 3题表格专项：Hybrid Recall@5=1.0000，MRR@10=0.6667，nDCG@10=0.7272
- LangGraph流程检查：工具准确率=1.0000，引用存在率=1.0000，证据Hit@5=1.0000，处理记录覆盖率=1.0000；该结果不代表回答事实准确率
- 20题应用层自查：Retrieval Recall@5=1.0000、事实字符串覆盖=0.9667、引用存在率=1.0000、5道不可回答/危险题拒答率=1.0000；评测准备状态=false
- 24题关键词流水线验收仍保留，但不再作为正式gold评测口径

结论：项目目前包含资料版本记录、正文和表格索引、混合检索、工具选择、范围和安全门控、引用检查、反馈、方案保存、应用层评测及并发保护。评测题数量和独立审核仍不达标，当前也没有外部模型结果。接入模型前应先扩充到60题以上并完成独立复核。
