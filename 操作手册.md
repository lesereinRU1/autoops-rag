# AutoOps RAG 操作手册

> 本手册面向公开 clone，所有主流程命令均从 repository root 执行，不依赖作者本机目录。当前 corpus、测试、评测和 Planner 状态统一以 [docs/current-status.md](docs/current-status.md) 为准。

## 1. 数据分发边界

Git 仓库包含代码、公开 seed、formal dataset、schema 和 canonical evaluation report，但不包含：

- `data/raw/` 中的原始手册；
- `data/processed/chunks.jsonl` 和表格缓存；
- `storage/autoops.db`；
- `storage/qdrant/`；
- 模型缓存；
- Trace 运行产物；
- React 的 `node_modules/` 与 `dist/`。

原因是资料许可、隐私、体积和本地运行状态不能混入代码仓库。要运行真实检索、Demo 或 formal evaluation，必须先准备合法资料并构建索引。

当前 canonical report 对应 6 个文档、16,945 chunks，其中正文 4,934、表格行 12,011，覆盖 1,856 张表和 1,064 个表格页。上述 corpus 的 SHA-256 为 `090f5e5f416ea1762d4f71e7a28b10d0e7f083ef30ae04f3a305c1b6b769a213`。该 hash 用于识别评测 corpus，不表示 Git 仓库分发这些数据。

## 2. 本地安装

当前主要验证环境：Windows PowerShell、Python 3.11。

```powershell
git clone https://github.com/lesereinRU1/autoops-rag.git
Set-Location autoops-rag
py -3.11 -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
Copy-Item .env.example .env
```

所有相对路径都由 `app/config.py` 基于 repository root 解析，因此仓库可以位于任意 clone 目录。

## 3. 准备资料与索引

### 3.1 使用项目下载脚本

仓库版本化的 `data/sources.json` 只保存公开下载来源元数据。只在确认来源和许可后执行：

```powershell
.\.venv\Scripts\python.exe scripts\download_data.py
```

脚本把 PDF 和本次下载得到的大小/hash 写入被 Git 忽略的 `data/raw/`，不会改写版本化来源清单。

可选资料：

```powershell
.\.venv\Scripts\python.exe scripts\download_data.py --include-optional
```

### 3.2 使用自己的资料

把允许使用的 `.pdf`、`.md`、`.txt`、`.html` 或 `.htm` 文件放入：

```text
data/raw/
```

不要把企业内部手册、现场日志、设备地址、用户问题或受版权限制资料提交到 Git。

### 3.3 构建 processed chunks 和 Qdrant

```powershell
.\.venv\Scripts\python.exe scripts\ingest.py --mode semantic
```

`.env.example` 默认使用 `EMBEDDING_BACKEND=hash`，便于无模型环境运行。要使用 BGE/FastEmbed，设置：

```text
EMBEDDING_BACKEND=fastembed
EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5
```

再重新执行 ingest。不同 Embedding 配置会生成不同索引；不能把不同 corpus/config 的指标直接与 canonical report 比较。

## 4. 启动后端

```powershell
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

检查：

- 原生兼容页面：<http://127.0.0.1:8000/>
- Swagger UI：<http://127.0.0.1:8000/docs>
- Liveness：<http://127.0.0.1:8000/health/live>
- Readiness：<http://127.0.0.1:8000/health/ready>
- Runtime Metrics：<http://127.0.0.1:8000/api/metrics/runtime>
- Prometheus-compatible Metrics：<http://127.0.0.1:8000/metrics>

Readiness 不通过时优先检查：

1. `data/processed/chunks.jsonl` 是否存在；
2. Qdrant collection 与 chunks 数量是否一致；
3. `.env` 的 Embedding backend/model 是否与建库时一致；
4. 运行数据库是否可连接；
5. raw/processed/storage 目录是否有权限。

## 5. 启动 React Demo

保持后端运行，在新 PowerShell 中执行：

```powershell
Set-Location frontend
npm ci
npm run dev
```

访问 <http://127.0.0.1:5173/demo/>。

Vite 默认把 `/api` 和 `/health` 代理到 `127.0.0.1:8000`。如果后端使用其他可信地址，复制 `frontend/.env.example` 并设置 `VITE_API_BASE_URL`。

生产构建验证：

```powershell
npm run typecheck
npm run build
```

构建后 `frontend/dist/` 可由 FastAPI 的 `/demo` 静态入口读取，但 `dist/` 是本地产物，不提交 Git。当前 Dockerfile 不负责构建 React。

当前 SSE 是工作流事件流，不是 Token Streaming。最终回答在 `completed` 事件一次性返回；取消按钮主要停止浏览器接收，已进入同步线程的检索或模型 I/O 可能短暂继续。

## 6. 受控 Planner

默认：

```text
ENABLE_AGENTIC_ROUTING=false
```

此时固定 LangGraph 处理真实请求，Planner/Router 只记录候选 Trace。

开启：

```powershell
$env:ENABLE_AGENTIC_ROUTING = "true"
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

真实 Planner 仍是确定性 `BoundedQueryPlanner`，不是 LLM Planner。Safety、Out-of-scope、明确故障码、明确参数和普通单步查询继续规则优先；表格定位、跨章节流程和版本核对等适用 intent 才进入 executor。

正式工具只有：

- `search_manual`
- `lookup_fault_code`
- `lookup_parameter`
- `get_document_page`

工具必须通过 Registry 参数 schema、budget 和 timeout。相同工具及规范化参数只真实执行一次；结构化工具不能绕过 Evidence Gate/Citation Guard；`get_document_page` 不能猜文档或页码。

## 7. 本地 MCP stdio Server

```powershell
.\.venv\Scripts\python.exe -m app.mcp.server
```

通常使用示例 Client 启动和验证：

```powershell
.\.venv\Scripts\python.exe examples\mcp_client.py
```

MCP 与 LangGraph 共享同一 `AutoOpsService` 和 `ToolRegistry`。当前只有本地 stdio transport，没有远程 HTTP MCP、认证、TLS 或多租户。

## 8. PostgreSQL Runtime Repository

默认 SQLite：

```text
DATABASE_BACKEND=sqlite
```

可选 PostgreSQL 只保存 conversation、feedback、verified solution、Trace metadata 和 evaluation record，不迁移 Qdrant 或静态领域知识。

启动本地 PostgreSQL：

```powershell
docker compose -f docker-compose.yml -f docker-compose.postgres.yml up -d postgres
$env:POSTGRES_DSN = "postgresql+psycopg://autoops:autoops-local-only@127.0.0.1:5432/autoops"
.\.venv\Scripts\python.exe -m alembic upgrade head
```

应用切换：

```powershell
$env:DATABASE_BACKEND = "postgres"
$env:POSTGRES_DSN = "postgresql+psycopg://autoops:autoops-local-only@127.0.0.1:5432/autoops"
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

专用 integration test 只接受数据库名包含 `test` 的 DSN：

```powershell
$env:AUTOOPS_POSTGRES_TEST_DSN = "postgresql+psycopg://autoops:autoops-local-only@127.0.0.1:5432/autoops_test"
.\.venv\Scripts\python.exe -m pytest -m postgres_integration
```

不要把真实密码或 DSN 写入 Git。

## 9. Docker

Docker 启动前必须满足数据前置条件：

1. `.env` 已存在；
2. 宿主机 `data/raw/` 有合法资料，或 Docker volumes 中已有一致的 processed chunks 和 Qdrant collection；
3. 如果已有 Qdrant 数据，先确认其与 chunks 一致；
4. React 不是当前 Docker image 的构建目标。

```powershell
Copy-Item .env.example .env
docker compose config --quiet
docker compose up --build -d
```

如果没有 raw、processed chunks 和可复用 collection，`index-init` 会失败并阻止空知识库应用启动。它不会隐式删除已有 Qdrant collection；明确重建前必须人工确认数据范围。

停止：

```powershell
docker compose down
```

## 10. 测试与评测

全量 Pytest：

```powershell
.\.venv\Scripts\python.exe -m pytest
```

最近一次真实默认结果：

```text
206 passed, 1 skipped
```

skip 为没有设置专用 PostgreSQL test DSN 的 integration test。

Formal dry-run：

```powershell
.\.venv\Scripts\python.exe scripts\run_formal_eval.py --dry-run --split test
```

真实 test split：

```powershell
.\.venv\Scripts\python.exe scripts\run_formal_eval.py --split test
```

真实执行需要当前 processed chunks、索引和 frozen dataset 可用。当前 canonical report 使用 `generation_mode=local_extractive`、`LLM_ENABLED=false`；不得把其 E2E 规则指标描述为外部 LLM Answer Quality。

当前结果和限制见：

- [docs/current-status.md](docs/current-status.md)
- [docs/evaluation.md](docs/evaluation.md)
- [docs/eval-summary.md](docs/eval-summary.md)
- [reports/formal_evaluation.md](reports/formal_evaluation.md)

## 11. 常见问题

### API 能启动但检索为空

确认已经执行下载/放置资料和 ingest，并检查 `/health/ready` 中的索引状态。Git clone 本身不包含 corpus。

### LLM 不可用

`LLM_ENABLED=false` 时本地抽取式回答是正常配置，不是错误。启用外部 OpenAI-compatible provider 时只在本地 `.env` 填写 `LLM_BASE_URL`、`LLM_API_KEY` 和模型名，不得提交真实 key。

### 为什么不是 Token Streaming

当前 LLM Client 使用非流式请求。SSE 只推送 LangGraph 工作流事件，`first_token_latency_ms` 实际表示完整响应可用耗时，不是真实 TTFT。

### 为什么不提供生产级承诺

当前是本地、只读、可复现的工程原型；formal test 只有 20 题，官方来源比例有限，Metrics 是单进程内存数据，MCP 是本地 stdio。项目不具备生产多实例、高可用或现场设备控制能力。

## 12. 可选 D 盘 helper

以下脚本保留作者本地工作流，内部仍假设仓库位于 `D:\autoops-rag`：

- `scripts/setup_d_drive.ps1`
- `scripts/start.ps1`
- `scripts/start_background.ps1`
- `scripts/stop.ps1`
- `scripts/rebuild_index.ps1`
- `scripts/check_status.ps1`
- `scripts/enable_bge.ps1`

它们是 optional local helper，不是官方安装、启动或验收入口。公开用户应使用本手册第 2～10 节的 repository-root 命令。
