# AutoOps RAG：D 盘完整操作手册

## 0. 先看当前已经完成的结果

项目实际位置是 `D:\autoops-rag`。本机已完成安装、下载、索引、评测和API验收。现在不需要从头安装，直接执行第2节中的启动命令即可。

当前数据与验收结果：

| 项目 | 实际值 |
|---|---:|
| 已入库官方 PDF | 3 份 |
| PDF 页数 | 25 + 1374 + 386 = 1785 页 |
| 中文补充资料 | 3 份 |
| 入库切片 | 当前重建 16945 个（正文4934 + 表格行12011） |
| 结构化表格 | 当前重建 1856 张，覆盖1064个文档页 |
| 中文向量 | `BAAI/bge-small-zh-v1.5`，512 维 |
| 向量库 | Qdrant 本地模式，数据在 D 盘 |
| 关键词流水线验收题 | 24 道 |
| 小规模 gold-chunk 回归集 | 15 道（5道官方手册题，10道项目补充资料题） |
| 全集 Hybrid Recall@5 / nDCG@10 | 1.000 / 0.892 |
| 官方手册子集 MRR@10 / nDCG@10 | 0.567 / 0.675 |
| 工具准确率 / 引用存在率 | 1.000 / 1.000 |
| 应用层自查题 | 20道；事实字符串覆盖0.967，拒答率1.000 |
| 自动测试 | 阶段 F 默认环境 180 项通过、1 项 PostgreSQL integration test 跳过；本阶段未重跑专用 PostgreSQL 环境 |

项目全部位于 D 盘。可运行 `scripts\check_status.ps1` 查看当前实时占用。

## 1. 每个地方分别做什么

### 1.1 Windows 文件资源管理器

用途：查看项目、复制 PDF、查看报告。

在地址栏输入：

```text
D:\autoops-rag
```

最常用位置：

- `data\raw`：放 PDF、Markdown、TXT、HTML 原始资料；
- `data\processed\chunks.jsonl`：当前重建后正文和结构化表格行共 16945 个切片；
- `data\processed\table_cache`：严格线框模式生成的表格解析缓存；
- `data\eval\questions.jsonl`：评测问题；
- `models`：BGE 模型缓存；
- `storage\qdrant`：向量索引；
- `storage\autoops.db`：故障码、参数、最近一次问答记录、反馈和已确认方案；
- `reports`：评测和服务器日志。

### 1.2 PowerShell

用途：安装、启动、停止、重建索引、运行测试。命令都在 PowerShell 中执行，不是在 Python 窗口、浏览器控制台或 TIA Portal 中执行。

打开方法：按Windows键，输入`PowerShell`并打开Windows PowerShell。普通权限即可，不要为了这个项目长期使用管理员权限。

每次新开 PowerShell，第一条命令都先进入项目：

```powershell
Set-Location D:\autoops-rag
```

如果系统提示不允许运行脚本，只对当前窗口临时放行：

```powershell
Set-ExecutionPolicy -Scope Process Bypass
```

这不会永久修改系统策略，关闭窗口后失效。

### 1.3 浏览器

用途：提问、查看证据、查看 API。

- 项目页面：<http://127.0.0.1:8000>
- React Demo：构建 `frontend/` 后访问 <http://127.0.0.1:8000/demo>
- 中文接口测试页：<http://127.0.0.1:8000/docs>
- 开发接口结构：<http://127.0.0.1:8000/swagger>
- 健康状态：<http://127.0.0.1:8000/health>

`127.0.0.1` 表示只允许本机访问。不要为了图方便把 host 改成 `0.0.0.0` 暴露给工厂网络。

### 1.4 VS Code（可选）

用途：看代码、改配置、编辑评测题。不是运行项目的必需软件。

在 PowerShell 中：

```powershell
code D:\autoops-rag
```

如果`code`命令不存在，可在VS Code的`文件`菜单中选择`打开文件夹`，再选择`D:\autoops-rag`。

Python 解释器选择：

```text
D:\autoops-rag\.venv\Scripts\python.exe
```

## 2. 每天怎么启动

### 方法 A：后台启动，推荐

在 PowerShell 中：

```powershell
Set-Location D:\autoops-rag
Set-ExecutionPolicy -Scope Process Bypass
.\scripts\start_background.ps1
```

服务启动后，在浏览器打开<http://127.0.0.1:8000>。

后台启动后可以关闭 PowerShell，服务仍会运行。服务器日志写在：

```text
D:\autoops-rag\reports\server.err.log
D:\autoops-rag\reports\server.out.log
```

### 方法 B：前台启动，适合观察日志

```powershell
Set-Location D:\autoops-rag
.\scripts\start.ps1
```

这个窗口必须保持打开。停止时按 `Ctrl+C`。

### 停止后台服务

```powershell
Set-Location D:\autoops-rag
.\scripts\stop.ps1
```

不要在服务运行时直接删除 `storage\qdrant`，否则会破坏正在使用的索引。

## 3. 浏览器里怎么提问

1. 在`问题`框输入问题；
2. 在型号下拉框选择资料范围：经典`S7-1200`对应V4.6英文手册，`S7-1200 G2`对应2025中文手册；不要为了得到中文而混用不同设备代际；
3. 已知固件版本时填写版本，不知道可以留空，但答案会提醒补充；
4. 选择`手册问答`；
5. 点击`开始查询`；
6. 先看回答，再向下核对证据文档、页码和`chunk_id`。

建议先用这些问题验收：

```text
S7-1200 使用 MB_CLIENT 出现 16#80C8，应该按什么顺序排查？
MB_CLIENT 和 MB_SERVER 分别是什么角色？
Modbus TCP 默认端口是多少？
40001 和协议报文地址为什么可能差一？
32 位浮点数为什么要占两个保持寄存器？
```

回答中的`[来源N：文档名，第x页]`可以由程序检查。点击`查看原文和技术标识`可以核对`chunk_id`、表格ID和原文。只有证据与问题相关时，才能把它当作排查依据。

## 4. 数据到底在哪里，不是空壳的证据

在文件资源管理器打开 `D:\autoops-rag\data\raw`，可以看到：

1. `siemens_modbus_tcp_s7_1200_s7_1500.pdf`：Siemens Modbus/TCP 专题资料，25 页；
2. `siemens_s7_1200_system_manual_v4_6.pdf`：经典 S7-1200 V4.6 系统手册，1374 页；
3. `siemens_s7_1200_g2_system_manual_2025_zh.pdf`：S7-1200 G2 V1.0.1 中文系统手册，386 页；
4. 三份中文结构化补充资料；
5. `sources.json`：官方地址、版本、发布日期、核验日期、状态、文件大小和 SHA-256。

经典S7-1200的V4.7是更新版本，但当前官方直链在本机环境不可稳定下载，因此V4.6被标记为`superseded-but-retained`。运行以下命令查看资料日期和已知缺口：

```powershell
.\.venv\Scripts\python.exe scripts\check_sources.py
```

原始资料经 PyMuPDF 解析后写入：

```text
D:\autoops-rag\data\processed\chunks.jsonl
```

每一行都包含 `chunk_id`、正文、原始页码、章节路径、型号、版本和来源 URL。向量写入：

```text
D:\autoops-rag\storage\qdrant
```

BM25 使用同一份 `chunks.jsonl` 构建。检索时 Dense 和 BM25 各取候选，RRF 融合，再进行轻量 Rerank。

## 5. 添加你自己的 PDF

### 第一步：在文件资源管理器复制文件

把文本型 PDF 复制到：

```text
D:\autoops-rag\data\raw
```

文件名尽量使用英文、数字、下划线，例如：

```text
vendor_drive_modbus_manual_v2_3.pdf
```

不要覆盖同名旧手册。不同版本要保留在不同文件中。

### 第二步：在VS Code或记事本补充来源信息

编辑 `D:\autoops-rag\data\raw\sources.json`，在最外层数组中增加一项：

```json
{
  "filename": "vendor_drive_modbus_manual_v2_3.pdf",
  "title": "某驱动器 Modbus 手册",
  "manufacturer": "制造商名称",
  "model": "设备型号",
  "version": "V2.3",
  "url": "原始下载地址或企业文档编号",
  "ingest": true,
  "license_note": "公司内部授权资料，仅限授权环境使用"
}
```

JSON 细节：上一项结尾必须有逗号，最后一项结尾不能多逗号；双引号必须是英文半角字符。

### 第三步：在PowerShell停止服务并重建

```powershell
Set-Location D:\autoops-rag
.\scripts\stop.ps1
.\scripts\rebuild_index.ps1 -Mode semantic
.\scripts\start_background.ps1
```

`semantic` 是语义切分；如果要做消融实验，可改成：

```powershell
.\scripts\rebuild_index.ps1 -Mode fixed
```

重建会删除并重新创建当前 collection，不要在中途强制关机。

如果曾强制终止重建，先保持网页服务停止并执行索引对账，清除不属于当前`chunks.jsonl`的旧points：

```powershell
.\.venv\Scripts\python.exe scripts\reconcile_index.py
```

输出中的`consistent`必须为`true`，然后再启动服务。

### 第四步：在浏览器检查结果

打开 <http://127.0.0.1:8000/health>，确认 `indexed_chunks` 与重建日志一致。再提 5～10 个能从新手册中找到明确答案的问题，逐条核对页码。

## 6. 扫描 PDF、表格和图片要注意

当前版本保存页面正文，同时把有明确线框的表格按行整理为`表名 + 表头=单元格值`，并保存表号、行号、页码和边界框。查询`80C8`、`MB_DATA_LEN`一类值时，表头和对应值不会被分到不同切片。

表格抽取使用严格线框、网格占用率和网址版式过滤。运行以下命令查看真实数量、分文档样本和误判检查：

```powershell
.\.venv\Scripts\python.exe scripts\table_audit.py
```

报告写入`reports\table_extraction_audit.json`。本轮由 `chunks.jsonl` 复核为16945个总切片、12011个表格行切片、1856张表；链接版式误判等质量结论仍应以重新执行审计脚本后的报告为准。

扫描件仍不能直接处理。判断方法：在 PDF 阅读器里能不能用鼠标选中并复制正文。

如果完全选不中，说明是扫描件，需要先 OCR。不要直接入库，否则可能得到 0 个有效切片。OCR 后要人工抽查：

- 型号中的 `0/O`、`1/I/l` 是否识别错误；
- 十六进制故障码是否丢失 `#`；
- 表格行列是否串位；
- 页码是否仍与原文件一致。

复杂寄存器表不能只看 OCR 文本，最终结论必须回到原表格核对。

## 7. 运行自动测试和检索评测

### 在PowerShell运行自动测试

```powershell
Set-Location D:\autoops-rag
.\.venv\Scripts\python.exe -m pytest
```

预期看到：

```text
180 passed, 1 skipped
```

默认跳过的是需要专用 PostgreSQL 测试 DSN 的 integration test。阶段 D 已完成过真实 PostgreSQL 验证；阶段 F 本轮没有设置 `AUTOOPS_POSTGRES_TEST_DSN`，因此不能把本轮默认测试写成新的 PostgreSQL 全量通过结果。

### 在PowerShell运行检索评测

```powershell
Set-Location D:\autoops-rag
.\.venv\Scripts\python.exe scripts\benchmark.py
```

脚本默认使用 `auto` 模式：如果网页服务正在运行，就通过 `http://127.0.0.1:8000/api/search` 评测，避免第二个进程重复打开本地 Qdrant；如果服务没有运行，才直接读取本地索引。输出中的 `mode` 会显示实际采用的方式。

若要强制通过服务评测：

```powershell
.\.venv\Scripts\python.exe scripts\benchmark.py --mode api
```

若要强制在本地进程中评测，必须先停止网页服务，否则 Qdrant 会报告文件已被另一进程锁定：

```powershell
.\scripts\stop.ps1
.\.venv\Scripts\python.exe scripts\benchmark.py --mode local
```

结果同时保存到：

```text
D:\autoops-rag\reports\retrieval_metrics.json
```

24道题是关键词流水线验收集，不能作为正式召回率证明。项目另提供15道小规模 `gold_questions.jsonl`，其中3道专门验证表格行召回；每题包含类别、目标 `gold_chunk_ids` 和预期工具。该数据集没有经过独立盲审，10题来自项目补充资料，只有5题来自官方手册。运行：

```powershell
.\.venv\Scripts\python.exe scripts\benchmark_gold.py
```

会同时输出 Dense、BM25、Hybrid 的Hit@5、Recall@5、MRR@10、nDCG@10、按类别指标和按资料来源拆分的结果，还会记录评测文件SHA256、运行编号、检索配置、工具准确率、引用存在率和处理记录覆盖率。当前仍只是小样本回归测试，正式扩展到100至300题时，应由独立的设备工程师审核正确证据和参考答案。

应用层自查评测：

```powershell
.\.venv\Scripts\python.exe scripts\benchmark_application.py --split all
```

该脚本读取带Schema和SHA256锁的20题数据集，分别统计检索、事实字符串、引用、工具选择、拒答和bad case。事实字符串覆盖不是语义正确率；报告中的评测准备状态必须达到true后，才能在简历中写成正式准确率。

多轮追问测试：

```powershell
.\.venv\Scripts\python.exe scripts\benchmark_memory.py
```

运行耗时和1/4/8并发测试：

```powershell
.\.venv\Scripts\python.exe scripts\benchmark_runtime.py --requests 12
```

结果分别写入`reports\memory_evaluation.json`和`reports\runtime_benchmark.json`。当前多轮测试只有3组；并发测试显示8并发P95为2406.08毫秒、吞吐量为4.668请求/秒。本地CPU向量计算和SQLite写入仍会增加尾延迟。

## 8. API 在哪里操作

在浏览器打开<http://127.0.0.1:8000/docs>，可以直接点击按钮读取服务状态、检索手册或生成回答。页面会显示HTTP状态码、耗时和实际结果。字段结构和自动生成的Swagger页面保留在<http://127.0.0.1:8000/swagger>，供开发检查。

主要接口：

| 接口 | 用途 |
|---|---|
| `GET /health` | 查看服务、后端和索引块数 |
| `POST /api/search` | 只做混合检索 |
| `POST /api/chat` | LangGraph 路由、检索和引用回答 |
| `POST /api/chat/stream` | 通过 SSE 展示工作流阶段，最后返回完整回答；不是 LLM Token Streaming |
| `GET /api/alarms/{code}` | 查结构化故障码 |
| `POST /api/solutions/verify` | 用户确认后保存解决方案 |
| `GET /api/metrics/runtime` | 以 JSON 查看当前进程的运行指标 |
| `GET /metrics` | 以 Prometheus 兼容文本读取运行指标 |

`save_verified_solution` 不会由模型自动调用。保存前程序会验证所有 `source_chunk_ids` 是否真实存在。

## 9. LangGraph 实际做了什么

当前真实调用链可以简化为：

```text
用户请求 -> FastAPI -> LangGraph -> 工具注册中心（Tool Registry）-> RAG/工具
        -> LLM 或本地证据摘要 -> 引用校验（Citation Guard）-> 返回答案
```

处理顺序如下：

1. 读取设备信息和故障码。
2. 查询相关实体和关系。
3. 固定路由选择查询类型，并通过工具注册中心（Tool Registry）执行 `search_manual`、`lookup_fault_code` 或 `lookup_parameter`；对外兼容字段仍可能显示 `lookup_alarm_code` 或 `check_parameter_range`。
4. 查询人工确认过的相似方案。
5. 执行 Dense、BM25、RRF 和 Rerank。
6. 证据充分性判断（Evidence Gate）不通过时，最多执行一次问题改写（Query Rewrite）。
7. 生成带来源编号的回答，并执行引用校验（Citation Guard）。
8. 记录用户反馈。
9. 人工确认方案后保存，后续相似问题可以再次使用并核对手册证据。

`save_verified_solution`只在用户明确确认方案有效后调用。页面显示工具选择原因、知识关系、检索方式、证据检查和回答方式。反馈统计可从`/api/metrics/business`查看，未收到真实反馈时保持0。

实现状态要分开理解：

- **已真实实现**：上述固定 LangGraph 主流程、Tool Registry、四个正式工具、本地 MCP stdio Server、React Demo、工作流级 SSE，以及默认 SQLite/可选 PostgreSQL 的数据访问层（Repository）。
- **shadow / experimental**：Intent Classifier、Tool Router、Bounded Query Planner 只记录候选决策；Iterative Retrieval 默认关闭。
- **尚未实现**：Planner 主动执行、开放式工具循环、远程 HTTP MCP、LLM Token Streaming 和生产级多实例监控。

### 会话范围和运行统计

最近1到2轮问题只在`这个参数`、`那写入呢`一类短追问中用于补全对象；不会把完整历史聊天重新加入下一次问题。记录24小时有效，可在页面手动清空。

当前未配置外部 LLM，不使用 KV cache，也没有外部 Token 费用。`POST /api/chat` 的 `runtime` 字段会返回总耗时、检索轮数、向量与 BM25 检索操作数、结构化查询次数、回答方式、外部模型调用次数和输入/输出 Token 数。模型接口失败时会记录降级机制（fallback）的原因并使用本地抽取式回答。`first_token_latency_ms` 当前应理解为“响应可用耗时（当前不代表真实 TTFT）”，因为 LLM 不是 Token Streaming。

## 10. 是否必须配置 LLM

不必须。当前已经能完整运行：检索、故障码工具、参数工具、LangGraph、引用和评测都可用。未配置 LLM 时，系统从证据中抽取句子生成保守回答。

如果以后要接 Qwen 等 OpenAI 兼容接口，在 `.env` 中填写：

```text
LLM_BASE_URL=https://你的兼容接口/v1
LLM_API_KEY=your_api_key_here
LLM_MODEL=模型名称
```

注意：

- 不要把 `.env` 提交到 Git；
- 企业内部手册是否允许发送到外部 API，要先看数据合规要求；
- 修改 `.env` 后要停止并重启服务；
- 生成质量变好不代表检索质量变好，仍要单独看 Recall、MRR 和引用。

## 11. C 盘空间不足时最容易踩的坑

本项目已把这些内容放在 D 盘：

- 项目：`D:\autoops-rag`；
- 虚拟环境：`D:\autoops-rag\.venv`；
- pip 缓存：`D:\autoops-rag\.cache\pip`；
- 临时目录：`D:\autoops-rag\.tmp`；
- Hugging Face / FastEmbed 模型：`D:\autoops-rag\models`；
- Qdrant：`D:\autoops-rag\storage\qdrant`。

不要在项目外直接运行 `pip install ...`，否则包可能装入其他 Python 或用户目录。始终使用：

```powershell
D:\autoops-rag\.venv\Scripts\python.exe -m pip ...
```

本项目默认不用 Docker，因为 Docker Desktop 的 WSL2 虚拟磁盘通常仍在 C 盘。若以后一定要用 Docker，先在 Docker Desktop 中把 disk image location 迁到 D 盘，再启用容器版 Qdrant。

查看当前占用：

```powershell
Set-Location D:\autoops-rag
.\scripts\check_status.ps1
```

确认运行稳定后，可以删除 `D:\autoops-rag\.cache\pip` 释放约 131 MB；不要删除 `.venv`、`models`、`storage` 或 `data`。

## 12. 从零重装时的完整顺序

只有换电脑或删除了虚拟环境时才做这一节。

1. 把项目完整复制到 `D:\autoops-rag`；
2. 打开 PowerShell；
3. 执行：

```powershell
Set-Location D:\autoops-rag
Set-ExecutionPolicy -Scope Process Bypass
.\scripts\setup_d_drive.ps1
```

脚本会依次：创建 D 盘虚拟环境、安装依赖、下载官方 PDF、构建 BGE 向量、写入 Qdrant、运行测试。

如果网络无法下载 Hugging Face 模型，先使用无需下载模型的完整降级版：

```powershell
.\scripts\setup_d_drive.ps1 -Minimal
```

网络恢复后升级：

```powershell
.\scripts\enable_bge.ps1
```

整个过程中不要关闭 PowerShell；首次模型下载可能出现短暂 SSL 重试，只要最终没有红色异常即可。

## 13. 常见故障

### 浏览器打不开 127.0.0.1:8000

先运行：

```powershell
.\scripts\check_status.ps1
```

若服务未运行，执行 `.\scripts\start_background.ps1`。若端口已被其他程序占用，运行：

```powershell
Get-NetTCPConnection -LocalPort 8000 -State Listen
```

不要盲目结束未知进程；确认后再处理。

### 提示 Qdrant 文件被占用

说明服务还在使用本地索引。先执行 `.\scripts\stop.ps1`，再重建索引。

### 新 PDF 入库后搜不到

依次检查：文件是否在 `data\raw`、是否有文本层、`sources.json` 的 `filename` 是否完全一致、`ingest` 是否为 `true`、重建时是否出现异常、`/health` 中切片数是否增加。

### 答案有引用但内容不对

引用存在只表示来源编号有效，不表示结论一定正确。查看证据原文和原PDF页，确认型号、版本、参数对象和上下文一致；把失败问题加入评测集后再调整切分和检索。

## 14. 工程上的下一步

当前版本适合演示和继续开发。正式用于生产前还要做：

1. 扩展到 10～20 份与你现场型号和固件一致的授权手册；
2. 建 200～300 道经人工审核的评测集；
3. 加入扫描 PDF OCR，并增强跨页表格和合并单元格归一化；
4. 按型号、固件、语言和文档版本做严格过滤；
5. 对不可回答、高风险操作和跨版本冲突做专门测试；
6. 做 1/4/8/16 并发、P50/P95、内存和索引大小测试；
7. 经过现场工程师评审后再接真实业务系统。

完成这些工作后，项目才适合在经过审核的场景中试用。
